{
  "name": "niji-local",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-only, mobile-friendly chat UI for Ollama and OpenAI-compatible model servers.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/main.js"
  },
  "engines": {
    "node": ">=18"
  }
}
